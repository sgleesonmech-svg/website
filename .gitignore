# OS
.DS_Store

# Local tooling (Claude Code preview server)
.claude/

# Original full-size source images (kept locally; the site uses the optimised
# copies in assets/img/). Remove these lines if you want them versioned too.
*.png
!assets/img/*.png
!assets/img/**/*.png
